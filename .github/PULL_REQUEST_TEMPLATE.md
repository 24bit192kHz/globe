## What this changes

<!-- One or two sentences: what does this PR do? -->

## Checklist

- [ ] `cargo fmt --all --check` passes
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo build --workspace --release` passes
- [ ] `cargo test --workspace` passes
- [ ] New/changed textures are committed as baked `.gidx` files (the baker
  never runs in CI)
- [ ] New/changed behavior is covered by the `CHANGELOG.md` `Unreleased`
  section where appropriate
- [ ] By opening this PR I agree to license my contribution under GPL-3.0
  (see `CONTRIBUTING.md`)

## Verification

<!-- Exact commands you ran and their output, e.g. `globe -s -t mars`,
`python3 tools/bake_textures.py --body europa`, `cargo test --workspace`.
See CONTRIBUTING.md for the full check list. -->

## Screenshots

<!-- For rendering changes: terminal size, glyph mode (`-G`), body (`-t`),
and a screenshot or pasted frame. Delete this section if not applicable. -->
